// The active modular flight computer entry point lives in src/main.cpp.
//
// This root-level file is retained as a harmless translation unit so older IDE
// references do not point at the previous monolithic prototype. Keeping it free
// of mission logic prevents a stale second simulator from contradicting the
// physics-based implementation in src/.
void rootMainTranslationUnitMarker()
{
}
